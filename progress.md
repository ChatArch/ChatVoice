# Progress

## 2026-08-17 会议录音前端改造

- 默认首页从模型能力实验台改为移动端优先的会议录音产品页
- 复用现有 `/ws/asr/stream`，浏览器麦克风音频实时发送到统一 ASR 通道
- 增加 MediaRecorder 本地归档、暂停/继续、结束和下载
- 增加“文字记录 / 实时摘要”，摘要调用现有 `/api/meeting-notes/polish`
- 保留 TTS、声音克隆、Realtime 与 ASR 后端路由，暂不在主页面暴露
- 更新前端合同检查，覆盖产品主界面、录音状态、WebSocket、摘要与密钥边界

## 下一步

- 已在本地启动 FastAPI，完成静态页、前端合同、转写提取和 ASR 合同检查
- 已推送 GitHub `main`（提交 `0df1bf0`），同步 Recall 并重启现有服务
- 已通过公网 HTTPS/WSS 验证：新版首页、识别设置、摘要页及 `funasr-gpu` 静音流完整返回
- 公网体验地址：`https://qwen-audio-demo.public.wzhecnu.cn/?v=0df1bf0`

## 当前发布级别

- 可作为内部/邀请制 Beta 使用：HTTPS、WSS、GPU ASR、服务端密钥与摘要接口均可用
- 尚不按正式生产服务承诺：当前进程由 tmux 守护，未配置登录、限流、监控告警与历史数据存储
- 现有流式 ASR 连接仍有 60 秒演示限制；长会议、本地恢复与完整录音二次校对尚待实现
- 下一步优先完成手机真机麦克风测试、连接续期/重连、IndexedDB 分片保存和 systemd/容器化守护

## 2026-08-17 实时转写修复

- 待机/静音波形改为平静直线，录音时由真实时域采样和静音阈值驱动
- 增加“清空”操作；结束或报错后可清除文字、摘要和本地录音并重新开始
- 修复结束录音 5 秒后提前关闭 WebSocket、导致 GPU 结果尚未返回便丢失的问题
- FunASR 进程内模型按设备缓存，避免每个自动分片重复冷启动模型
- 服务端增加分片开始/完成、字数与耗时日志，不记录识别正文
- 增加 `scripts/check_meeting_e2e.py`，以 Qwen TTS 音频验证自动分片、GPU ASR 文本相似度和 Qwen 摘要
- Recall 已改用 GPU venv 直接运行 Web 服务；模型预热后每个 3 秒分片约 65–89 ms
- 最终 Qwen TTS 端到端回归：14 秒音频自动生成 5 个分片，合并文字相似度 0.9624，Qwen 摘要成功

## 2026-08-17 上下文回写

- 流式 ASR 每 3 秒对当前会话音频进行累积重识别，以后文修正最近的识别假设
- WebSocket 结果增加 `revision`、`revision_scope=session`、`replace` 与 `final`；前端按 revision 原位覆盖而非重复追加
- 前端按中文标点自动整理段落，最近两段显示“回写中”，结束时切换为最终确认文本
- 波形振幅上限降至容器高度的 38%，增加 0.76/0.24 缓动，并提高静音阈值以抑制环境底噪
- Qwen TTS 回归得到 5 次累积 revision、最终 3 个语义段落，合并文本与原文相似度 1.0，摘要成功

## 2026-08-17 双模式会话管理

- 增加 Open WebUI 风格左侧会议栏：新会议、搜索、今天/更早分组、切换和删除
- 首页增加访客/账号两种入口；访客会议只写入浏览器 IndexedDB，登录会议才写入服务器
- 增加账号注册、登录、会话恢复与退出接口；密码使用 PBKDF2-SHA256 加盐哈希
- 登录态使用 HttpOnly Cookie，会议写入和删除要求 CSRF token；匿名服务器会议请求返回 401
- 服务器 SQLite 按账号隔离会议标题、转写与摘要，原始录音默认不上传保存
- 修复清空后的摘要回写竞态：清空时中止摘要请求并用内容版本拦截过期响应
- 串行化会议保存，保证清空写入不会被较早的摘要保存覆盖
- 新增 `scripts/check_meeting_storage_contract.py`，覆盖匿名隔离、注册、Cookie 会话、CSRF、账号隔离、增删改查和退出

## 2026-08-17 实时区域与结束状态修复

- 将“已确认记录”和“实时识别”拆成两个视觉区域，实时内容使用独立浅色卡片和分隔标题
- 累积 revision 的最后一个临时句只显示在实时卡片，不再同时复制到历史列表
- 点击结束后保留实时卡片并显示“正在提交”，直到最终 revision 返回后才并入历史记录
- 连接错误或正常结束时会提交仍在实时卡片中的最后文字，避免状态切换造成视觉清空
- 录音期间每 5 秒补充一次会议保存，结束时立即保存最终文本

## 2026-08-17 实时对话模型与历史

- 实时对话页增加服务端驱动的模型下拉；当前 Token Plan 账号仅返回 `qwen-audio-3.0-realtime-plus`
- WebSocket 代理接受经过校验的 `model` 参数，后续账号开放新 Realtime 模型时前端会自动列出
- 左侧栏在实时对话页切换为对话管理，支持新建、搜索、打开和删除
- 访客对话文字保存在 IndexedDB；登录对话按账号写入服务器 SQLite，均不保存原始音频
- 增加当前对话 Markdown 导出，包含模型、音色和按轮次排列的文字
- 修复用户最终转写晚于助手事件到达导致的显示倒序：语音开始时先建立用户消息占位，再原位回填识别结果

## 2026-08-17 会议转写三区状态机

- 正式记录、上下文回写和实时识别使用独立状态与独立视觉区域
- 正式记录只追加；短 revision 保留上一版回写和实时草稿，避免窗口反复缩短
- 暂停不提交或清空文字；结束后再次录音会在同一会议继续追加
- 新增 `scripts/check_transcript_state_cases.js`，覆盖 13 类边界场景与 40 步单调性压力测试

## 2026-08-17 长会议与显式时长策略

- 移除早期公开 Demo 的 60 秒隐藏限制
- 访客入口与录音控制区明确展示每个录音段 10 分钟；登录用户展示每段 2 小时；暂停不计时
- 服务端根据 HttpOnly 登录 Cookie 下发并强制执行 600 / 7,200 秒策略，浏览器不自行声称更高额度
- ASR 从整场累积改为约 42–45 秒滚动上下文，窗口最终文字向正式记录单向提交，长会议不会持续增长 GPU 输入和服务端音频内存
- 到达单段上限时正常生成最后 revision 和 `done`，不会以连接错误结束；结束后及历史会议均可继续追加新录音段
- 自动化完成 2 小时 PCM 模拟、100 次以上窗口轮换、精确上限、暂停不计时、访客/账号鉴权策略和窗口协议回归
- 原始录音从 JS 数组改为每秒写入独立 IndexedDB；仅点击下载时读取并组装 Blob，浏览器存储失败时明确提示并降级到内存分片
